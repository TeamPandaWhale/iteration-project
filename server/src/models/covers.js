export const idIndex = new Map();
export const gameIdIndex = new Map();

export const loadCovers = () => {};

const Covers = {};

Covers.findOneById = (id) => {};

Covers.findOneByGameId = (gameId) => {};

Covers.findAllByIds = (ids) => {};

Covers.findAllByGameIds = (gameIds) => {};

export default Covers;
